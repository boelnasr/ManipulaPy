# Core documentation dependencies
sphinx>=8.0.0,<9.0.0
pydata-sphinx-theme>=0.18.0,<0.20.0
sphinx-rtd-theme>=3.0.0,<4.0.0
myst-parser>=4.0.0,<5.0.0

# For better API documentation
sphinx-autodoc-typehints>=3.0.0,<4.0.0
sphinx-copybutton>=0.5.2,<1.0.0

# For rendering the executed teaching notebooks (requires pandoc; see .readthedocs.yaml)
nbsphinx>=0.9.0,<1.0.0
nbconvert>=7.17.1            # 6.x calls the removed jinja2.utils.escape; needs 7.x for Jinja2 3.1+
ipython>=8.39.0

# For mathematical expressions
sphinxcontrib-katex>=0.9.11,<1.0.0

# Basic scientific computing (NumPy 2-compatible docs build)
numpy>=2.0,<3.0
matplotlib>=3.9.0,<4.0.0

# Required for proper documentation build
packaging>=26.3
# Must stay inside Sphinx's own range: Sphinx 8.x requires docutils<0.22, so a
# >=0.23 floor makes this file unresolvable rather than merely outdated.
docutils>=0.20,<0.22

# For proper reStructuredText parsing
Pygments>=2.21.0,<3.0.0
